name: Roadmap date fix (archived)

# A correção já foi aplicada ao index.html.
# Este workflow fica apenas como registro e não é executado automaticamente.
on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Correção do campo de data do Roadmap já aplicada."
